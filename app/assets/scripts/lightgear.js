/*global jQuery, document */

(function ($) {

    'use strict';

    $(document).ready(function () {

        // initialize widgets
        var toolbar = $('#toolbar').toolbar();

    });
}(jQuery));
